# Seat-Select Worker 部署配置
# 复制此文件为 .env.deploy.worker 并根据实际情况修改

# 内网服务器 SSH 配置
WORKER_USER=msun
WORKER_HOST=192.168.192.57
WORKER_PORT=22
WORKER_SSH_KEY=~/.ssh/id_ed25519

# 部署路径 (远程服务器上的目录)
WORKER_DEPLOY_PATH=/home/msun/Projects/seat-select
WORKER_COMPOSE_FILE=docker-compose.worker.yml

# 镜像配置
REGISTRY_URL=registry.woohelps.com
VERSION=latest
